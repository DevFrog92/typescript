// function sum(a, b) {
//   return a + b;
// }

// sum();


// @ts-check

/**
 *
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */
function sum(a, b) {
  return a + b;
}

// jsdoc에서는 vscode에서 잡아낼 수 없다.
sum(10, '30');